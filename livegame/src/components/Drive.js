const Drive = ({ play }) => {

    // return component
    return (
        <span className="badge bg-info text-light supersmall">
            Drive {play.drive}
        </span>
    )
}

export default Drive